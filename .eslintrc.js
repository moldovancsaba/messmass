// WHAT: ESLint configuration for MessMass project
// WHY: Enforce design system compliance and code quality standards
module.exports = {
  extends: ["next"],
  plugins: ["react"],
  rules: {
    // All rules enabled - proper fixes applied instead of suppressions
  }
};
