'use client';

/**
 * AI Analytics workspace page
 * WHAT: Route shell for the AI analytics view.
 * WHY: Matches the other analytics pages, which compose a view component.
 */

import AiAnalyticsView from './AiAnalyticsView';

export default function AiAnalyticsPage() {
  return <AiAnalyticsView />;
}
