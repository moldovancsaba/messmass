'use client';

/**
 * AI event report page
 * WHAT: Route shell for the per-event AI report.
 */

import AiEventReportView from './AiEventReportView';

export default function AiEventReportPage() {
  return <AiEventReportView />;
}
