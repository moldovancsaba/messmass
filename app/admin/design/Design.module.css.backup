/* Design.module.css - Design System Management Page Styles */

/* WHAT: Centralized styles for design management page
 * WHY: Replace inline styles with proper CSS module using design tokens
 * HOW: Consistent layout patterns matching other admin pages */

/* WHAT: Horizontal layout container for style items
 * WHY: Replaces inline style for display:flex, justifyContent:space-between
 * HOW: Flex row layout with space-between, top-aligned items */
.styleHorizontalLayout {
  display: flex;
  gap: var(--mm-space-4);
  justify-content: space-between;
  align-items: flex-start;
}

/* WHAT: Content area on left side with flexible width
 * WHY: Replaces inline style for flex:1, minWidth:0
 * HOW: Flex grow to fill available space, min-width prevents overflow */
.styleContentArea {
  flex: 1;
  min-width: 0;
}

/* WHAT: Color preview circle for style display
 * WHY: Replaces inline style for background display
 * HOW: Inline-block circle with consistent spacing */
.styleColorCircle {
  display: inline-block;
  margin-top: var(--mm-space-2);
}
